1. docker compose build
2. docker compose up
3. open PaperlessAPI sln file
4. open package manager console inside visual studio
5. run command: Update-Database

api = localhost:8081

ui = localhost:80

pgadmin = localhost:8888
email = admin@admin.com
password = password
(connection host of db is the container name e.g. local_pgdb)

db = localhost:5432
user = admin
password = password

